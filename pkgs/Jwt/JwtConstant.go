package Jwt

const (
	TokenExpired     string = "Token is expired"
	TokenNotValidYet string = "Token not active yet"
	TokenMalformed   string = "That's not even a token"
	TokenInvalid     string = "Couldn't handle this token"
)
